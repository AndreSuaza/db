// tslint:disable-next-line:class-name
export class Card {
    id?: string;
    idCard?: string;
    index?: number;
    origin?: number;
    name: string;
    img: string;
    text?: string;
    // tslint:disable-next-line:no-inferrable-types
    use?: string = '';
}
